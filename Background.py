#testing

# ~ import pygame, sys, math

# ~ import pygame, sys, math

# ~ class Background(bg):
    # ~ def __init__(self, pos=[25,25]):
        # ~ spriteSheet = SpriteSheet("Vision board/maybe/FLATSTONES.png")
        # ~ self.image = images_at()
        # ~ self.rect = self.image.get_rect(center = pos)
        # ~ self.kind = "bg"
        
    # ~ def update(self, size):
        # ~ pass
